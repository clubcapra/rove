syntax = "proto3";

import public "Common.proto";

package rove.control_board;

service Bridge {
    rpc SetColor (rove.common.Color) returns (rove.common.Empty);

    rpc SetFrontLight (rove.common.State) returns (rove.common.Empty);


}