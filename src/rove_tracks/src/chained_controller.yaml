rove_tracks:
  track_joint_name: {
    type: string,
    default_value: "",
    description: "The name of the track joint.",
    read_only: true,
    # validation: {
    #   not_empty<>: null,
    # }
  }
  front_joint: {
    type: string,
    default_value: "",
    description: "Specifies the joint at the front of the track.",
    read_only: true,
    # validation: {
    #   not_empty<>: null,
    #   # unique<>: null,
    # }
  }
  back_joint: {
    type: string,
    default_value: "",
    description: "Specifies the joint at the back of the track.",
    read_only: true,
    # validation: {
    #   not_empty<>: null,
    #   # unique<>: null,
    # }
  }
