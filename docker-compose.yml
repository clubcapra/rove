version: '3'
services:
  devcontainer:
    build:
      context: .
      dockerfile: ./.devcontainer/Dockerfile
    volumes:
      - ./:/workspace/rove
    env_file:
      - .env
    command: sleep infinity
    network_mode: "host"
    privileged: true
